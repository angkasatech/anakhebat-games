{
  "name": "anakhebat-games",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "deploy": "npm run build && wrangler pages deploy dist --project-name anakhebat-games --branch main"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "vite": "^7.3.0",
    "wrangler": "^4.135.0"
  },
  "dependencies": {
    "@fontsource-variable/quicksand": "^5.3.0",
    "@fontsource/fredoka-one": "^5.3.0",
    "@fontsource/noto-naskh-arabic": "^5.3.0"
  }
}
